#include<stdio.h>
#include<locale.h>
#include<math.h>
#include<stdlib.h>
#define N 10

void main()
{
    setlocale(LC_ALL, "RUS");
    float A[N], temp;
for (int i = 0; i < 10; i++)
{
    printf("a[%d] = ", i + 1);
    scanf_s("%f", &temp);
    A[i] = temp;
}
puts("---------------------------");
puts("| индекс | исходное значение | новое значение");
puts("---------------------------");
a= .
printf("\n a[%d]= %d", i, a[i]);

puts("---------------------------");
}
